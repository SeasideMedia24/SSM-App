# components/

Reusable UI building blocks (buttons, forms, layout, the kanban board, etc.).
Keep components small and named for what they do. Client components only where
interactivity actually needs them; everything else stays a server component.
